[target.x86_64-unknown-linux-gnu]
# Due to the fact that `ideapad` requires read/write access to `/proc/acpi/call`, something that only root can write on,
# we'll need to do this workaround.
runner = "sudo -E"